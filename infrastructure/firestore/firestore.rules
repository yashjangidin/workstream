rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Privileged operational data is API-only through Firebase Admin.
    match /{document=**} { allow read, write: if false; }
  }
}
